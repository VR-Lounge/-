# 🚀 Развертывание Telegram бота в облаке

## ❌ Важные ответы

### 1. Firebase для бота
- ✅ **Можно** использовать Firebase Cloud Functions
- ⚠️ **НО** это сложнее и требует настройки
- 💰 **Бесплатный лимит:** Очень щедрый! Для небольшого VR клуба более чем достаточно
- 📝 Требует переписывания кода под Cloud Functions

**Бесплатные лимиты Firebase (Spark Plan):**
- **Cloud Firestore:** 50,000 чтений, 20,000 записей, 10,000 удалений в день
- **Cloud Functions:** 125,000 вызовов в месяц
- **Хостинг:** 1 ГБ хранилища, 10 ГБ трафика в месяц

**Для небольшого VR клуба (10-20 клиентов/день, 5-10 записей/день):**
- ✅ Использование: ~100-200 операций чтения/записи в день
- ✅ Это всего **0.2-0.4%** от бесплатного лимита!
- ✅ **Вывод:** Бесплатного лимита более чем достаточно! 🎉

### 2. Премиум Telegram
- ❌ **НЕ подходит** для ботов
- ℹ️ Премиум - только для пользователей (больше места для медиа, синхронизация)
- ❌ **НЕ предоставляет** облачное хранилище для ботов
- ❌ Боты работают через Bot API, а не через премиум функции

---

## ✅ ЛУЧШИЕ ВАРИАНТЫ (рекомендуется)

### 🥇 Вариант 1: Railway.app (РЕКОМЕНДУЕТСЯ)

**Плюсы:**
- ✅ **БЕСПЛАТНО** (есть бесплатный тариф)
- ✅ Очень просто настроить (5 минут)
- ✅ Автоматический деплой из GitHub
- ✅ Автоматический перезапуск при ошибках
- ✅ Работает 24/7

**Минусы:**
- ⚠️ На бесплатном тарифе может быть ограничение по времени работы

**Инструкция:**
1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Создайте новый проект
4. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `ADMIN_GROUP_ID`
5. Загрузите `firebase-service-account.json` как секрет
6. Railway автоматически запустит бота!

---

### 🥈 Вариант 2: Render.com

**Плюсы:**
- ✅ **БЕСПЛАТНО**
- ✅ Просто настроить
- ✅ Автоматический деплой из GitHub

**Минусы:**
- ⚠️ На бесплатном тарифе сервис может "засыпать" после 15 минут бездействия
- ⚠️ Первый запрос после "сна" может быть медленным

**Инструкция:**
1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройте:
   - Build Command: `npm install`
   - Start Command: `node telegram-bot.js`
5. Добавьте переменные окружения
6. Загрузите `firebase-service-account.json`

---

### 🥉 Вариант 3: Firebase Cloud Functions

**Плюсы:**
- ✅ Интеграция с Firebase
- ✅ Масштабируемость

**Минусы:**
- ❌ **Платно** (после бесплатного лимита)
- ❌ Требует переписывания кода
- ❌ Сложнее настроить

**Если хотите использовать Firebase Functions:**
- Нужно переписать бота под Cloud Functions
- Использовать Webhook вместо Long Polling
- Настроить HTTPS endpoint

---

### 🇷🇺 Вариант 4: Yandex Cloud (Российский сервис)

У Яндекса есть **два варианта** для развертывания бота:

#### 4.1. Yandex Cloud Functions (Serverless)

**Плюсы:**
- ✅ **Бесплатный период:** 3000₽ на первый месяц
- ✅ Автоматическое масштабирование
- ✅ Оплата только за использование
- ✅ Интеграция с другими сервисами Yandex Cloud

**Минусы:**
- ❌ **Требует переписывания** кода под Serverless архитектуру
- ❌ Нужно использовать Webhook вместо Long Polling
- 💰 Платно после бесплатного периода
- ⚠️ Сложнее настроить

**Когда использовать:**
- Если нужна serverless архитектура
- Если планируете масштабирование
- Если готовы переписать код

**Ресурсы:**
- [Практикум по разработке Telegram-бота](https://education.yandex.ru/knowledge/praktikum-razrabotka-telegram-bota-s-ispolzovaniem-serverless)
- [Статья на Хабре](https://habr.com/ru/articles/522126/)
- [Документация Yandex Cloud Functions](https://cloud.yandex.ru/docs/functions/)

---

#### 4.2. Yandex Compute Cloud (VPS) ⭐ РЕКОМЕНДУЕТСЯ ДЛЯ РФ

**Плюсы:**
- ✅ **От 200₽/месяц** (очень доступно!)
- ✅ **Российский сервис** - быстрая поддержка на русском
- ✅ Можно использовать **текущий код** без изменений
- ✅ Полный контроль над сервером
- ✅ Стабильность и надежность
- ✅ Хорошая документация на русском языке

**Минусы:**
- ⚠️ Требует базовых знаний Linux
- ⚠️ Нужно настроить сервер самостоятельно

**Когда использовать:**
- ✅ Если нужен российский сервис
- ✅ Если хотите использовать текущий код без изменений
- ✅ Если нужна стабильность и контроль

**Пошаговая инструкция:**

1. **Регистрация в Yandex Cloud**
   - Перейдите на [cloud.yandex.ru](https://cloud.yandex.ru)
   - Зарегистрируйтесь (нужен Яндекс ID)
   - Активируйте бесплатный период (3000₽ на первый месяц)

2. **Создание виртуальной машины**
   - Перейдите в раздел "Compute Cloud" → "Виртуальные машины"
   - Нажмите "Создать виртуальную машину"
   - Выберите:
     - **Платформа:** Intel Cascade Lake
     - **Ядра:** 2 vCPU
     - **Память:** 2 GB RAM
     - **Диск:** 20 GB SSD
     - **ОС:** Ubuntu 22.04 LTS
   - Настройте сеть (оставьте по умолчанию)
   - Добавьте SSH ключ для доступа

3. **Подключение к серверу**
   ```bash
   ssh ubuntu@<IP_АДРЕС_СЕРВЕРА>
   ```

4. **Установка Node.js**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   node --version  # Проверка версии
   ```

5. **Установка Git**
   ```bash
   sudo apt-get update
   sudo apt-get install -y git
   ```

6. **Клонирование репозитория**
   ```bash
   git clone https://github.com/VR-Lounge/-.git
   cd "-"
   ```

7. **Установка зависимостей**
   ```bash
   npm install
   ```

8. **Настройка переменных окружения**
   ```bash
   nano .env
   ```
   Добавьте:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен
   ADMIN_GROUP_ID=-1002640127163
   ```

9. **Загрузка Firebase ключа**
   ```bash
   nano firebase-service-account.json
   ```
   Вставьте содержимое вашего `firebase-service-account.json`

10. **Установка PM2 для автозапуска**
    ```bash
    sudo npm install -g pm2
    pm2 start telegram-bot.js --name vr-lounge-bot
    pm2 save
    pm2 startup
    ```
    Скопируйте и выполните команду, которую выдаст `pm2 startup`

11. **Проверка работы**
    ```bash
    pm2 logs vr-lounge-bot
    pm2 status
    ```

**Стоимость:**
- Минимальная конфигурация: ~200-300₽/месяц
- Средняя конфигурация: ~400-500₽/месяц
- Бесплатный период: 3000₽ на первый месяц

**Документация:**
- [Yandex Compute Cloud](https://cloud.yandex.ru/docs/compute/)
- [Создание виртуальной машины](https://cloud.yandex.ru/docs/compute/quickstart/quick-create-linux)

---

### 🏆 Вариант 5: Другие VPS провайдеры

**Плюсы:**
- ✅ Полный контроль
- ✅ Стабильность
- ✅ Нет ограничений

**Минусы:**
- 💰 Платно (от 200-500₽/месяц)
- ⚠️ Требует настройки сервера

**Провайдеры:**
- Timeweb (от 200₽/месяц)
- Selectel (от 300₽/месяц)
- DigitalOcean (от $5/месяц)

---

## 📋 Файлы, которые нужны для развертывания

### Обязательные файлы:
1. ✅ `telegram-bot.js` - основной файл бота
2. ✅ `package.json` - зависимости проекта
3. ✅ `.env` - переменные окружения (или настройте в облаке)
4. ✅ `firebase-service-account.json` - ключ Firebase (загрузите как секрет)

### Файлы, которые НЕ нужны:
- ❌ `node_modules/` - установится автоматически
- ❌ `bot.log` - логи будут в облаке
- ❌ `.gitignore` - уже настроен

---

## 🚀 Быстрый старт с Railway.app (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Подготовка файлов

Убедитесь, что у вас есть:
- ✅ `telegram-bot.js`
- ✅ `package.json`
- ✅ Все файлы загружены в GitHub

### Шаг 2: Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий `vr-lounge.github.io/-/`

### Шаг 3: Настройка переменных окружения

В Railway добавьте переменные:
- `TELEGRAM_BOT_TOKEN` = ваш токен бота
- `ADMIN_GROUP_ID` = `-1002640127163`

### Шаг 4: Загрузка Firebase ключа

1. В Railway перейдите в "Variables"
2. Добавьте переменную `FIREBASE_SERVICE_ACCOUNT`
3. Скопируйте содержимое `firebase-service-account.json` (весь JSON)
4. Вставьте в значение переменной

### Шаг 5: Обновление кода бота

Нужно немного изменить `telegram-bot.js` для работы с переменной окружения:

```javascript
// Вместо:
const serviceAccount = require('./firebase-service-account.json');

// Используйте:
let serviceAccount;
if (process.env.FIREBASE_SERVICE_ACCOUNT) {
  serviceAccount = JSON.parse(process.env.FIREBASE_SERVICE_ACCOUNT);
} else {
  serviceAccount = require('./firebase-service-account.json');
}
```

### Шаг 6: Настройка запуска

В Railway:
- **Start Command**: `node telegram-bot.js`
- **Build Command**: (оставьте пустым или `npm install`)

### Шаг 7: Деплой

Railway автоматически запустит бота после сохранения настроек!

---

## ✅ Проверка работы

После деплоя:
1. Проверьте логи в Railway (вкладка "Deployments" → "View Logs")
2. Отправьте `/start` боту в Telegram
3. Проверьте, что бот отвечает

---

## 🔧 Решение проблем

### Бот не отвечает
- Проверьте логи в Railway
- Убедитесь, что переменные окружения установлены
- Проверьте токен бота

### Ошибка Firebase
- Убедитесь, что `FIREBASE_SERVICE_ACCOUNT` правильно загружен
- Проверьте формат JSON (должен быть валидным)

### Ошибка подключения
- Убедитесь, что интернет доступен
- Проверьте, что Firebase проект активен

---

## 📝 Итого

**Для начала:** Используйте **Railway.app** - это самый простой и бесплатный вариант.

**Для продакшена (российский сервис):** Используйте **Yandex Compute Cloud** - доступно, стабильно, русская поддержка.

**Альтернатива:** Если Railway не подходит, используйте **Render.com** или **VPS**.

**НЕ используйте:** Премиум Telegram - это не для ботов.

